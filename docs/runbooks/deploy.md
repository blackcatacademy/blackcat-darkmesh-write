# Deploy Runbook (placeholder)

- Validate code and schemas: `scripts/verify/preflight.sh`.
- Build and publish AO process bundle (TBD once packaging is defined).
- Apply configuration for process IDs, roles, and capability policies.
- Smoke test commands with sample fixtures (see `fixtures/`).
